# v4.3.3 目前球員同步修正版

## 問題
按下「下一位」或「換對手」後：
- 下方多人追分記分板與雙方即時得分已更新
- 上方「目前球員」仍停留在上一位

## 原因
目前球員卡片在部分流程中於切換球員前更新，
而且不同函式各自操作同一個畫面元素，造成狀態不同步。

## 修正
新增唯一的 `renderCurrentPlayerCard()`：
- 每次 `drawQuick()` 最後才更新目前球員
- 使用切換完成後的 `activeParticipantIndex`
- 同步更新球員姓名、單次擊球時間與暫停狀態
- 單打、正式比賽、多人追分皆使用同一套更新流程
