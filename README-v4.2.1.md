# v4.2.1 計時畫面修正

## 問題
單打、多人追分與正式比賽使用快速紀錄畫面 `drawQuick()`，
但 v4.2 的背景 timer 只更新獨自練球畫面 `draw()`，
因此單次擊球時間資料有累積，畫面數字卻停在 00:00。

## 修正
新增 `updateActiveClock()`：
- 獨自練球更新 `draw()`
- 單打、正式比賽、多人追分更新 `drawQuick()`

所有模式現在都會每 500 毫秒刷新單次擊球時間。
